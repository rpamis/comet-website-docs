---
title: '多 Change 与规格冲突'
description: '并行管理多个 Native change，理解当前归属、工作区选择和安全的归档顺序。'
---

一个项目可以同时推进多个 Native change。`.comet/current-change.json` 只选择下一次写入归属哪个 workflow/change，不限制 active change 数量。Runtime 在归档时按 capability 串行处理，避免一个 change 擅自覆盖另一个 change。

## 并行 change 与工作目录

创建新 change 时，Native 会检查当前目录和已注册的 worktree；适用时一次展示 `current`、`branch`、`worktree` 选项和推荐。当前目录已被其他 active change 使用时，通常推荐 `worktree`。每个 change 会记住自己的 branch/worktree 绑定，错误目录恢复会停下来。

已有 change 不会被自动搬到新 worktree。跨设备继续前，先同步代码和正式 Native 文件，再确认 artifact root、branch/worktree 一致。

## 查看并选择

```bash
comet native status
comet native status <change-name> --details --json
comet native select <change-name>
```

`status` 和 `show` 是只读的，不会擅自切换当前 change。多个候选、selection 缺失或指向另一套 workflow 时，写入和恢复会返回 `await-user`，不会猜测。

## 哪些工作适合并行

不同 capability、没有共享行为约束的 change 可以并行。开始前确认：

- 是否修改同一个 capability 的 Spec；
- 是否都需要修改同一用户文档或配置；
- 一个 change 的验收是否依赖另一个尚未归档的行为；
- 当前工作目录是否混入了其他 change 的修改。

Runtime 能发现当前已注册 worktree 中的 capability 重叠，但它不是跨远端分支或跨设备的团队锁。没有同步的代码和 worktree 仍需要团队自己协调。

## 同一 capability 的归档顺序

如果两个 change 都要修改同一 capability，Archive 会返回一个需要串行顺序的 blocker。先选定哪个 change 归档，再让另一个 change 读取归档后的 canonical Spec，必要时执行：

```bash
comet native spec rebase <change-name> --summary <调整摘要>
```

rebase 会让目标 Spec 对齐最新行为，并要求重新 Build 和 Verify。不要直接把后归档的文件覆盖到前一个 change，也不要复用冲突前的通过结论。

## 跨设备协作

同步项目代码、`.comet/config.yaml`、brief、Specs、`comet-state.yaml` 和已有的 `verification.md`。本机 `.comet/runtime/native/` 可以缺失，Runtime 会重建；如果两个设备的 artifact root、branch/worktree 不一致，恢复会等待用户修正。

继续阅读：[产物与状态](/zh/native/artifacts-and-state)、[验证与修复](/zh/native/verification-and-repair) 和 [恢复手册](/zh/native/recovery-playbook)。
