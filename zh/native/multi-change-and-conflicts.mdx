---
title: "多 Change 与规格冲突"
description: "使用 Native Supervisor Change 模式或独立 Change 管理大型需求、并行工作和规格冲突。"
---

Native 支持两种多 Change 方式：对一个大型目标使用 Supervisor Change 模式，或分别推进多个相互独立的 Change。选择哪种方式取决于需求之间是否可以独立实现和验收。

## 什么时候使用 Supervisor Change 模式

当一个目标至少包含两个可以独立实现、独立验证的结果，并且存在真实的依赖关系或并行价值时，Native 会在 Shape 阶段建议 Supervisor Change 模式。需求文字很长、任务数量很多，但目标仍然紧密耦合时，不会仅因为规模而拆分。

Supervisor Change 负责整体目标、依赖关系和最终验收；每个 child 都是一个完整的 Native Change，可以单独恢复和验证。

## 使用 Supervisor Change 模式

在 Agent 中从统一入口开始：

```text
/comet-native
```

描述完整目标，例如：

```text
为管理后台增加权限 API、权限配置页面和审计日志，并分别验证这三个结果。
```

Shape 阶段会：

1. 判断目标是否适合拆分；
2. 展示候选 child、依赖顺序和验收覆盖；
3. 等待你确认拆分、调整方案或保持为单个 Change。

确认前不会创建 child、worktree 或派发 Agent。确认 Supervisor 拆分时，用户只选择一次多会话协作（推荐）或单会话推进；选择会写入 `comet-state.yaml`，恢复或重新进入 Shape 时不再重复询问。确认后，Runtime 会从当前可执行的 `readyChildren` 开始推进。选择多会话时优先使用独立会话或 Agent Team；这些能力不可用或恢复时已经丢失，则自动改用 subagent，但不会自动切换为单会话。subagent 也不可用时，流程会明确报告阻塞。

每个 child 都在独立 worktree 中完成 Build 和 Verify，并在 Archive 时按顺序合并回 Supervisor Change 的分支。所有 child 完成后，Supervisor Change 会在最终集成结果上重新 Verify，确保整体目标仍然满足。

查看 Supervisor Change 和 child 的状态：

```bash
comet native status <supervisor-change> --details --json
```

正常推进时，按照 Runtime 返回的 continuation 执行下一步，不要手工创建重复的 child 或 worktree。恢复 `/comet-native` 时，Comet 会复用已有状态。

如果 Supervisor Change 的最终 Verify 失败，已归档的 child 不会被重新打开。Runtime 会提示创建覆盖失败验收项的 repair child，确认 Supervisor Change Shape 后继续。

## 多个独立 Change 与工作目录

如果几个目标没有共同的整体验收，可以直接创建多个独立 Change。创建 Change 时，Native 会根据当前目录和已有 Change 提供 `current`、`branch` 和 `worktree` 选项：

- 串行处理且当前目录干净时，可以使用 `current`；
- 需要独立分支但不需要同时使用多个目录时，可以使用 `branch`；
- 并行开发、多个会话或当前目录已有未提交改动时，使用 `worktree`。

Native 会复用已经登记的匹配 worktree；如果分支仍然存在但 worktree 已被移除，会在恢复时重建它。每个 Change 都会保留自己的分支和工作目录绑定，错误 checkout 不会被静默使用。

## 查看、选择和恢复

日常推进用不到这些命令，Skill 自己会读状态。多个 change 并行时想手动看一眼、或需要明确写入归属，才用：

```bash
comet native status
comet native status <change-name> --details --json
comet native select <change-name>
comet native doctor <change-name>
```

`status`、`show` 和 `doctor` 是只读的，不会替你切换 Change。多个候选、缺少选择或工作目录不匹配时，Runtime 会等待明确决定。

## 处理规格冲突

不同 capability 且没有共享行为约束的 Change 可以并行。开始前确认：

- 是否修改同一个 capability 的 Spec；
- 是否都需要修改同一份用户文档或配置；
- 一个 Change 的验收是否依赖另一个尚未归档的行为。

如果多个 Change 修改同一个 capability，Archive 会要求确定顺序。先归档的 Change 成为新的 canonical Spec，后续 Change 需要重新读取；如果 Runtime 返回重新对齐（rebase）动作，按返回的 continuation 执行，再重新 Build 和 Verify。不要直接覆盖前一个 Change 的文件，也不要复用冲突前的通过结果。

## 跨设备继续

同步项目代码、`.comet/config.yaml`、brief、Specs、`comet-state.yaml` 和已有的 `verification.md`。本机 `.comet/runtime/native/` 可以缺失，Runtime 会重建；如果 artifact root、分支或 worktree 不一致，恢复会等待明确修正。

继续阅读：[Native 快速开始](/zh/native/quickstart)、[产物与状态](/zh/native/artifacts-and-state) 和 [验证与修复](/zh/native/verification-and-repair)。
