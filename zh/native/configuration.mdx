---
title: "Native 配置"
description: "了解 Native 的推荐配置、各字段的实际影响，以及语言、澄清、记忆、项目知识和交付方式如何设置。"
---

Comet 把项目配置保存在根目录的 `.comet/config.yaml`。运行 `comet init` 时会生成可用配置，大多数项目不需要手工调整。只有你想改变默认工作流、文档语言、澄清方式、记忆策略或交付方式时，才需要修改它。

## 推荐的 Native 配置

下面这份配置适合使用中文文档的新项目：

```yaml
schema: comet.project.v1
default_workflow: native
workflows:
  - native
ambient_resume: true

memory:
  learning: true
  retrieval: true

knowledge:
  provider: local

native:
  artifact_root: docs
  language: zh-CN
  clarification_mode: batch
  archive_confirmation: automatic
  max_verify_failures: 5
```

这会让 `/comet` 默认进入 Native，把 change 文档保存在 `docs/comet/`，使用中文生成正式产物，并以 Batch 模式完成需求澄清。

<Tip>
  优先使用 `comet init --workflow native` 创建配置，使用 `comet update` 补齐或升级托管字段。不要仅在 YAML 中加入一个工作流名称来代替 Skill、Rule 和 Hook 的安装。
</Tip>

## 入口与恢复

| 字段 | 默认值 | 对你的影响 |
| --- | --- | --- |
| `schema` | `comet.project.v1` | 配置格式版本，由 Comet 管理，不要手工更改 |
| `default_workflow` | 初始化时选择 | 决定 `/comet` 默认进入 Native 还是 Classic |
| `workflows` | 初始化时选择 | 声明项目启用了 `native`、`classic` 或两者 |
| `ambient_resume` | `true` | 自然语言看起来在继续旧任务时，运行只读恢复探测 |

`default_workflow` 只影响入口，不会把已有 change 从 Native 转成 Classic，也不会把 Classic change 转成 Native。

如果需要同时启用两套工作流，运行：

```bash
comet init --workflow both
```

要关闭自然语言触发的恢复探测，可以设置：

```yaml
ambient_resume: false
```

显式调用 `/comet` 时仍会按照 `default_workflow` 路由。

## Native 核心字段

| 字段 | 允许值 / 默认值 | 对你的影响 |
| --- | --- | --- |
| `native.artifact_root` | 项目相对路径；默认 `docs` | change、Spec 和报告保存在 `<artifact-root>/comet/` |
| `native.language` | `en` \| `zh-CN`；默认 `en` | 决定新 change 的 brief、Spec 和报告语言 |
| `native.clarification_mode` | `batch` \| `sequential`；默认 `batch` | 决定每轮集中询问还是一次询问一个选择点 |
| `native.archive_confirmation` | `automatic` \| `required`；默认 `automatic` | 决定 Verify 通过后是否额外等待归档确认 |
| `native.max_verify_failures` | 正整数；默认 `5` | 达到连续失败上限后暂停 Loop，等待用户处理 |

### 产物目录

`artifact_root: docs` 对应 `<project>/docs/comet/`；`artifact_root: .` 对应 `<project>/comet/`。路径必须位于项目内。

已有项目需要迁移目录时，不要直接改 YAML 并手动搬文件。让 Runtime 完成检查、切换和恢复记录：

```bash
comet native root show
comet native root move docs
```

### 文档语言

`native.language` 决定之后新建 change 的正式产物语言。已有 change 会继续使用创建时记录的语言，不会因为配置变化而自动改写。

```yaml
native:
  language: zh-CN
```

### 澄清模式

- `batch`：一次提出本轮所有前提已经明确、彼此独立的问题，适合减少往返。
- `sequential`：每轮只提出一个最上游或影响最大的问题，适合答案会改变后续问题的需求。

两种模式都会先调查仓库事实，并在进入 Build 前要求你确认完整的目标、范围、选择点、验收标准和非目标。切换模式不会清除 `brief.md` 中尚未回答的 `[blocking]` 问题。

### 归档确认

- `automatic`：最终 Verify 通过后，Runtime 可以继续完成归档。
- `required`：最终 Verify 通过后，再等待你明确同意归档。

`automatic` 只控制是否额外等待归档确认，不会替你授权 merge、push 或创建 PR。使用 branch 或 worktree 时，Comet 仍会让你选择实际的交付方式。

### Verify 失败上限

`max_verify_failures` 限制同一份已确认需求可以经历多少个失败实现轮次。达到上限后，Native 会暂停并让你决定继续、修改需求或停止，不会无限重试。

Runtime 执行错误、缺少外部信息和无法安全恢复的状态会单独返回 `await-user` 或 `blocked`，不应通过修改计数绕过。

## 个人记忆

`memory` 同时适用于 Native 和 Classic，两个开关默认都是 `true`：

| 字段 | 作用 |
| --- | --- |
| `memory.learning` | 允许 Comet 从工作流结果中整理可复用的个人偏好与协作经验 |
| `memory.retrieval` | 允许 Comet 在相关任务中检索并注入已有个人记忆 |

例如，只使用已有记忆但不继续自动学习：

```yaml
memory:
  learning: false
  retrieval: true
```

## 项目知识

`knowledge.provider` 默认是 `local`。Comet 会使用内置项目语料，你也可以追加项目中的 Markdown 文档：

```yaml
knowledge:
  provider: local
  local:
    include:
      - docs/architecture/**/*.md
      - packages/*/README.md
```

`knowledge.local.include` 只接受项目内的 Markdown glob。路径使用 `/`，不能越出项目根目录；这些路径会追加到内置语料，而不是替换它。

如果团队已经部署 Comet Retrieval API v1，可以切换为远程 Provider：

```yaml
knowledge:
  provider: remote
  remote:
    endpoint: https://knowledge.example.com
    token_env: COMET_KNOWLEDGE_TOKEN
    scope: engineering
    timeout_ms: 5000
```

`endpoint` 是你的服务实际接收请求的完整 URL。Comet 会直接向这个地址发送 `POST`，不会自动追加 `/comet` 或其他路径；示例域名需要替换为你的真实接口地址。

远程地址必须使用 HTTPS；只有 localhost 和其他 loopback 地址可以使用 HTTP。`token_env` 填环境变量名，不要把 Token 直接写进配置。超时时间允许 `100` 到 `30000` 毫秒。

## Hook 写入放行

`hook.allow_paths` 是 Native 与 Classic 共用的高级配置。它允许指定目录在原本受保护的阶段继续写入，默认为空。

```yaml
hook:
  allow_paths:
    - docs/team-notes
```

路径必须是项目相对目录，并使用 `/`。配置 `docs/team-notes` 会同时放行其下所有文件。

<Warning>
  这个配置会绕过对应目录的阶段写入限制。不要为了避免 Shape、Verify 或 Archive 的正常拦截而放行源码目录。`.comet`、Native 正式产物区和 Classic 正式产物区仍受专用规则保护，不能通过 allow list 绕过。
</Warning>

## 自定义 PR 创建命令

只有仓库必须使用自己的 PR 模板生成器或远端校验脚本时，才需要配置 `native.finish.pull_request`：

```yaml
native:
  finish:
    pull_request:
      provider: repository-command
      command: [pwsh, -NoProfile, -File, scripts/comet-create-pr.ps1]
      timeout_ms: 120000
```

仓库命令负责 PR 标题、正文、模板和仓库特有校验。Comet 仍负责提交、推送、查找已有 PR、核对 base/head/head SHA 和失败恢复。

注意以下限制：

- `provider` 当前只支持 `repository-command`。
- `command` 必须是非空参数数组；首项使用 PATH 中的命令名或项目相对路径，不能使用绝对路径。
- `timeout_ms` 默认 `120000`，最大 `600000`。
- 命令在项目根目录执行，通过 stdin 接收 `comet.native.pull-request-finish-input.v1` JSON，并通过 stdout 返回 `comet.native.pull-request-finish-result.v1` JSON。
- 即使配置自定义命令，也需要安装并认证 GitHub CLI（`gh`），供 Comet 独立核验远端状态。

未配置时，Comet 使用兼容的 `gh pr create --fill` 流程。

## 不要手工配置的字段

当前 Native v4 不再把 `native.snapshot` 写入用户配置。Runtime 仍能读取旧配置以完成兼容和迁移，但 `comet update` 会移除这个旧块。不要为了调整验收范围重新添加它。

`native.pending_root_move` 是产物目录迁移期间的 Runtime 恢复状态，也不应手工创建或修改。旧版 change-local Runtime、receipt 和 evidence 文件同样只用于迁移或兼容；运行 `comet update` 和 `comet doctor` 处理它们。

继续阅读：[用户选择点与需求澄清协议](/zh/native/decision-ownership)、[产物与状态](/zh/native/artifacts-and-state) 和 [恢复手册](/zh/native/recovery-playbook)。
