---
title: 'Native Loop 工程原理'
description: '理解 Native 如何用 Runtime 检查、新 Verifier 和有界计数，把模型自述完成变成可恢复的验收循环。'
---

普通 Agent loop 让模型自己决定“做完了没”。Native Loop 把完成判定交给 Runtime：模型负责理解和实现，Runtime 负责检查、分派验收、记录缺口和停止循环。

## 一次 Loop 怎么走

```text
Shape → Build handoff → Runtime checks → fresh Verifier
                         ↑                 ↓
                    repair gaps ← fail     pass → Archive
```

每轮都使用完整的验收列表 A1…An。Builder 的自报结果只作为上下文，不能代替 Verifier。Verifier 漏项、重复项、未知 ID 或检查失败都会被 Runtime 拒绝，并把具体缺口交给下一轮 Build。

## 什么算进展

Runtime 保存有限的 history 和计数：

- `iteration`：一次实现修复轮次；
- `attempt`：当前 Verify 尝试；
- `no_progress_count`：未解决验收没有严格减少的次数；
- `failed_iteration_count`：实现失败轮次；
- `execution_failure_count`：Runtime/Verifier 执行错误次数。

减少未解决验收项、让失败检查变为通过、补齐明确缺失的信息，才算真实进展。只改说明、重复同一命令、把失败项换个顺序，不能清零停滞计数。连续错误或没有进展达到上限后，状态进入 `await-user` 或 `blocked`。

## 检查只做必要的工作

必要检查由 Runtime 解析和执行。相同候选下已完成的计划会复用，额外检查通过当前 attempt 的 `request-checks` 请求，并有轮数和重复预算。stdout/stderr 流入本机日志，报告保留摘要；长日志不会单独使结果失效。Archive 正常路径不重新执行检查。

## 状态和恢复

`comet-state.yaml` 保存 Loop 的稳定摘要和下一步，`verification.md` 展示用户可读报告；本机 `state.json`、日志、锁和事务位于 `.comet/runtime/native/`。换设备时本机执行视为中断，Runtime 从 YAML 重建必要状态；不会把模型记忆或旧的本机 overlay 当成完成依据。

## 为什么要停下来

Runtime 在这些情况停止并返回明确动作：

- 仍有产品决定需要用户确认；
- 代码、branch/worktree 或 artifact root 不匹配；
- acceptance 未完整覆盖，或必要检查失败；
- Verifier 不可用、执行错误达到上限，或语义结果需要用户解决；
- 并行 change 要修改同一 capability，需要决定归档顺序。

这让 Loop 有终点：要么所有验收通过并归档，要么把剩余决定和风险交给用户，而不是无限消耗 Agent 轮次。

继续阅读：[验证与修复](/zh/native/verification-and-repair)、[连续推进](/zh/native/continuation-and-checkpoints) 和 [实战演练](/zh/native/walkthrough)。
