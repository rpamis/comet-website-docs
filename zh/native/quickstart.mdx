---
title: "Native 快速开始"
description: "用 /comet 启动一次 Native 变更，让 Comet 管理工作区、推进实现、独立验收并从中断恢复。"
---

你只需要告诉 Comet 想达成什么结果，并确认真正影响产品行为的决定。Comet 会把需求和验收标准保存在项目中，准备工作区，推进实现与独立验收，再完成归档。

Native 适合 Fable 5、GPT-5.6 及同档强模型。它不规定模型必须怎样计划或写代码，但不会把模型的一句“完成了”当作交付证据。

## 开始前

- Node.js 22 或更高版本
- Git
- 一个[受支持的 AI 编码平台](/zh/platforms)

## 三步开始一次 Native 变更

<Steps>
  <Step title="安装并初始化 Comet">
    在项目根目录运行：

    ```bash
    npm install -g @rpamis/comet
    comet init --workflow native
    ```

    初始化会安装 Native 所需的 Skill 和 Rule；支持 Hook 的平台还会安装统一 Router。项目配置会保存在
    `.comet/config.yaml`，新的 Native 文档默认放在 `docs/comet/`。

  </Step>

  <Step title="告诉 Comet 你想完成什么">
    打开 Agent，在项目中输入：

    ```text
    /comet 为用户资料页增加头像上传能力
    ```

    `/comet` 会读取项目配置并进入 Native。它是 Agent 中的 Skill 入口，不是终端命令。

    创建 change 时，你可以继续使用当前目录，也可以让 Comet 创建分支或 worktree。明确要求并行处理时，
    Comet 会使用独立 worktree 隔离任务。

  </Step>

  <Step title="确认目标，然后让 Comet 推进">
    Native 会先调查仓库里已经能确定的事实，只询问仍会改变范围、默认行为、兼容性或风险的决定。你确认目标、
    非目标和验收标准后，Comet 会继续实现、检查、独立验收和归档，不需要你手动切换阶段。
  </Step>
</Steps>

## Comet 会替你处理什么

- **准备并绑定工作区**：创建或复用分支和 worktree，恢复时回到正确目录；工作区不匹配时停止写入，避免代码落到错误目录。
- **持续推进工作流**：模型负责调查和实现，Runtime 决定当前阶段与下一步。只有出现用户决定或阻塞时才暂停。
- **独立判断是否完成**：Runtime 先运行必要检查，再由新的只读 Verifier 对照验收标准检查结果；发现缺口就带着明确问题回到 Build 修复。
- **从中断处继续**：需求、进度和验收结论保存在项目文件中。换窗口、换会话或上下文压缩后，不需要重新讲一遍背景。
- **带入相关经验**：启用 Personal Memory 和 Project Knowledge 后，Comet 会按当前任务选择相关的个人偏好与项目知识，不会一次塞入全部历史内容。

Native 的主路径只有四个阶段：

```mermaid
flowchart LR
  S["Shape\n确认目标"] --> B["Build\n实现与检查"]
  B --> V["Verify\n独立验收"]
  V -->|通过| A["Archive\n归档与交付"]
  V -->|发现缺口| B
```

## 中断后继续

重新打开项目后，在 Agent 中输入：

```text
/comet 继续
```

Comet 会重新读取 change、工作区绑定和下一步，从稳定断点继续。代码、分支或 worktree 对不上时，它会停下来说明问题，不会静默换一个目录推进。

想快速查看当前阶段，可以运行 `comet status`；想在浏览器中查看 Change、任务、验收和工作区进度，可以使用
[`comet dashboard`](/zh/dashboard/overview)。安装或状态异常时，再运行 `comet doctor` 排查。

```bash
comet status      # 查看所有 change 和当前阶段
comet dashboard   # 在浏览器中查看详细进度
comet doctor      # 检查安装、配置和状态
```

## 完成后你会得到什么

除了实现代码，项目中还会留下可读的需求 brief、完整目标 Specs、工作流状态和验证报告。下一位 Agent 可以从这些文件继续工作，不需要依赖原对话，也不用猜“上一次到底做到哪里”。

继续阅读：[Native 工作流](/zh/concepts/native-workflow)、[Supervisor Change](/zh/native/supervisor-change)、[产物与状态](/zh/native/artifacts-and-state) 和 [恢复手册](/zh/native/recovery-playbook)。
