---
title: 'Native 快速开始'
description: '为 Fable 5、GPT-5.6 及同档强模型初始化 Native，并完成一次可恢复的需求变更。'
---

Native 面向 Fable 5、GPT-5.6 及同档强模型。它把计划和实现方法交给模型，把完成判定交给 Runtime。Builder 不能靠一句“完成”结束工作：Runtime 会运行必要检查，再分派新的只读 Verifier 覆盖全部验收项。

## Native 的四阶段

```mermaid
flowchart LR
  S["Shape：需求与决定"] --> B["Build：实现与交接"]
  B --> C["Runtime checks：必要检查"]
  C --> V["Verify：新 Verifier"]
  V -->|通过| A["Archive：更新 Spec 并归档"]
  V -->|失败| B
```

- **Shape**：调查仓库事实，澄清真正影响产品行为的决定，写 brief 和完整目标 Spec。
- **Build**：模型自主选择实现、测试和修复方式，完成后提交 Builder handoff。
- **Verify**：Runtime 执行并复用必要检查，新的只读 Verifier 逐项检查 A1…An；失败就带着明确缺口回 Build。
- **Archive**：通过后应用 Spec、写入最终报告并归档；正常路径不重复运行 Verify。

## 1. 初始化项目

Comet 需要 Node.js 22 或更高版本。安装 CLI 后在项目根目录运行：

```bash
npm install -g @rpamis/comet
comet init --workflow native
```

新项目的 Native 产物默认放在 `docs/comet/`，机器 Runtime 固定在 `.comet/runtime/native/`。

共享配置位于 `.comet/config.yaml`：

```yaml
schema: comet.project.v1
default_workflow: native
workflows:
  - native
ambient_resume: true
native:
  artifact_root: docs
  language: zh-CN
  clarification_mode: batch
  archive_confirmation: automatic
  max_verify_failures: 5
```

## 2. 从统一入口开始

在 Agent 中输入：

```text
/comet 为用户资料页增加头像上传能力
```

`/comet` 按 `default_workflow` 确定性进入 `/comet-native`。如果要直接进入 Native，可以使用 `/comet-native`；它是 Skill 入口，不是 shell 命令。

## 3. 澄清真正的产品决定

Native 先调查可查事实，只把仍会改变范围、输出、默认行为、兼容性、风险或不可逆结果的问题交给用户。新项目默认 Batch，一轮询问当前前置条件已经确定的问题；Sequential 则每轮只问一个最上游问题。两种模式都要在进入 Build 前确认共享理解。

## 4. 让 Loop 按 Runtime 继续

阶段之间的推进由 Skill 完成：它读取 Runtime 返回的 `continuation` 决定下一步，不靠对话记忆，也不需要你手工衔接。你参与的只有 Shape 阶段的问答和确认。Verify 失败会回 Build，iteration 增加；相同缺口反复出现、执行错误达到上限或出现冲突时，Runtime 会返回 `await-user` 或 `blocked`，这时才会来问你。

如果需要额外检查，当前 Verifier 通过 `request-checks` 请求 Runtime 执行；不要自行写入“通过”的结果。能提供独立 subagent 的宿主会启动新的 Verifier execution；没有独立 execution 时，Runtime 会明确标记降级并在 Archive 前等待一次用户确认。

## 5. 中断之后

会话中断、换窗口或换设备后，你不需要执行任何恢复命令。重新输入 `/comet`，或者直接说“继续刚才的工作”，Skill 会重读磁盘上的 brief、Specs 和 `comet-state.yaml`，从断点接着推进——恢复是 Skill 和 Runtime 的事，不是你的操作步骤。

下面两个命令是可选的，只在你想自己看看进度、或怀疑安装和状态有问题时才用：

```bash
comet status   # 查看 change 列表和当前阶段
comet doctor   # 安装或状态看起来不对时做诊断
```

跨设备恢复需要你做的只有一件事：把这些文件随代码一起同步——`.comet/config.yaml`、brief、Specs 和 `comet-state.yaml`。`verification.md` 缺失时 Runtime 会重建；本机 `.comet/runtime/native/` 缺失也会由 Runtime 重建。代码、分支或 worktree 对不上时，恢复会停下来等你处理，不会换一个目录继续。

完成后留下的用户可读产物是 brief、Specs、`comet-state.yaml` 和 `verification.md`；本机 state、日志、锁和事务不属于 change 文档。

继续阅读：[产物与状态](/zh/native/artifacts-and-state)、[验证与修复](/zh/native/verification-and-repair)、[澄清模式](/zh/native/clarification-modes) 和 [恢复手册](/zh/native/recovery-playbook)。
