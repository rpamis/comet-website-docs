---
title: 'Native 工作流'
description: '面向强编码模型的自包含需求工作流：详细约束、轻量执行、可恢复状态与安全归档。'
---

Comet Native 面向能够自主调查代码、选择实现方式并按风险安排验证的强编码模型。它保留 Spec 模式真正有价值的部分——需求管理、change 生命周期、状态检查、自动推进、验证证据、并发冲突和可恢复归档——但不再规定固定的设计、计划、TDD、调试或审查方法。

它的原则是：**需求和验收条件要详细，执行协议要轻。** 模型需要知道目标、边界、决策、完整目标行为和验证期望；至于怎么实现，由模型结合仓库事实自主决定。

## Native 与 Classic 是两套独立工作流

|          | Native                                       | Classic Spec 模式                             |
| -------- | -------------------------------------------- | --------------------------------------------- |
| 适用模型 | 能自主规划和验证的强模型                     | 需要更明确步骤指导的模型                      |
| 需求产物 | Comet 自有 brief、完整目标规格和 change 状态 | OpenSpec change/delta spec + Superpowers 文档 |
| 执行约束 | 只约束结果、证据和安全边界                   | 五阶段 Skill、设计/计划/TDD/审查协议          |
| 依赖     | 只依赖 Comet 自带 Native runtime             | 依赖 Classic、OpenSpec 和 Superpowers 组合    |
| 入口     | `/comet-native`、`comet native ...`          | `/comet-classic` 及 Classic 阶段 Skill        |

`/comet` 是配置驱动的统一别名：它只读取项目根 `comet.config.yaml`，再转发到 `/comet-native` 或 `/comet-classic`。它不会根据任务大小、文件数量或模型判断切换模式，也不提供 Native 到 Classic 的升级或转换。两边不会共享 change、状态或归档目录。

## 初始化与默认入口

新的 Comet 项目运行项目级初始化时默认选择 Native：

```bash
comet init
```

这条路径只安装 Comet 自有 Skill/runtime，创建 `comet.config.yaml` 和默认的 `comet/` 目录，不安装 OpenSpec、Superpowers、CodeGraph，也不写入 Classic rules、hooks 或 `.comet/config.yaml`。已有明确 Classic 状态的项目继续保持 Classic；普通 `openspec/` 或 `docs/superpowers/` 目录本身不会被误判为 Classic。

需要显式选择时使用：

```bash
comet init --workflow native
comet init --workflow native --root docs
comet init --workflow classic
```

显式改变 `default_workflow` 只改变 `/comet` 的入口，不迁移、删除或转换任何已有 change。`--root` 只适用于项目级 Native；global 安装仍表示安装全局能力，不写项目配置。

## 目录与 artifact root

Native 只使用 Comet 自有目录：

```text
<project>/comet.config.yaml
<artifact-root>/comet/
  specs/
  changes/
  archive/
  runtime/
```

默认 artifact root 是项目根，因此产物位于 `<project>/comet/`。也可以初始化到 `docs`，得到 `<project>/docs/comet/`：

```bash
comet init --workflow native --root docs
```

`artifact_root` 必须是项目内相对路径。`comet native init --language zh-CN` 还会持久化以后新建 change 的默认语言，单个 `new --language` 可以覆盖它。需要调整位置时使用 `comet native root move`，不能直接编辑配置；迁移过程带事务状态，可在中断后继续或回滚。

## 四个阶段

```mermaid
flowchart LR
  A["Shape：理清需求"] --> B["Build：自主实现"]
  B --> C["Verify：记录证据"]
  C -->|pass| D["Archive：更新规格并归档"]
  C -->|fail| B
```

### Shape：让信息足以完成任务

Shape 将用户目标整理为 `brief.md`，固定记录 Outcome、Scope、Non-goals、Acceptance examples、Constraints and invariants、Decisions、Open questions 和 Verification expectations。

模型先调查仓库中能获得的事实，只把真正会改变范围、用户可见行为、兼容性、风险或不可逆性的决定交给用户。存在这类决定时，一次只问最重要的一个问题，并给出推荐答案、理由与各选项的实际影响；用户回答后把结论写入 Decisions，并用 `next --confirmed` 推进。没有高影响未知项时直接继续，由 runtime 记录隐式确认。`[blocking]` 问题无论哪种确认方式都必须先解决。

若变更会改变长期行为，还要为每个 capability 在 `changes/<change>/specs/<capability>/spec.md` 写一份**完整目标规格**：它描述归档后该能力应有的全部行为，而不是只能叠加在旧文本上的增量 patch。runtime 根据 canonical spec 推导 `create` / `replace` 并冻结 `base_hash`；删除使用 `comet native spec remove`。模型不直接维护这些状态元数据。

### Build：强模型自主选择方法

进入 Build 后，模型选择满足 brief 和目标规格的最简单可靠方案。是否写实现计划、采用何种测试粒度、如何调试和审查，都按任务风险决定。Native 不要求为了流程制造额外文档，也不强制 TDD。

若实现中发现新的高影响决定，模型把它标成 `[blocking]`，一次只问一个；用户回答后更新 Decisions、移除阻塞项，并在离开 Build 时用 `--confirmed` 记录。Build 会重新检查 brief 和目标规格，不能靠产物引用绕过新问题。

阶段推进需要引用真实项目产物；纯文档或无需代码的变更则记录明确的 no-code 原因。

### Verify：事实而不是声明

Verify 对照 Acceptance examples、完整目标规格和风险执行验证。`verification.md` 记录实际命令与结果、跳过项、规格一致性、已知限制和结论，并包含 Runtime 可解析的验收机器块。每个 Acceptance ID 都要绑定证据或明确的跳过理由。

Runtime 会把报告绑定到当时的 contract、implementation scope 和 change revision。brief、目标规格或实现文件变化后，旧 pass 和旧 check receipt 会变成 stale，不能继续归档。失败会自动返回 Build；未运行的检查不能写成通过。

模型可以自主修复，但 Runtime 不允许无进展地无限循环：相同 scope 下第三次出现相同失败签名时转为 manual stop，单个 repair episode 最多记录 12 次 failure。真实 scope 进展会开启新的 episode；无进展 override 只有一次。

### Archive：冲突安全地收敛

只有 Verify 为 pass 时才能归档。归档必须先 `archive --dry-run` 取得内容绑定的 `preflightHash`，再通过 `archive --expect-preflight <sha256>` 提交。Runtime 在锁内重算证据、路径和 canonical spec 的 SHA-256：

- `create` 要求 canonical spec 不存在；
- `replace` 和 `remove` 要求当前 hash 与 runtime 冻结的 `base_hash` 一致；
- hash 已变化时停止并报告冲突，不覆盖另一个 change 的结果。

冲突不是死路：重读最新 canonical spec、改写完整目标规格后运行 `comet native spec rebase`。runtime 刷新 operation/hash，把 change 受控重开到 Build 并清除旧验证结论，随后重新实现、确认和验证。

成功后完整目标规格成为 canonical spec，active change 移到 `archive/YYYY-MM-DD-<change-name>/`，trajectory 保留 Shape、Build、Verify 和 Archive 的摘要证据，但不保存隐藏推理过程。源树和目标文件使用有界、对象身份绑定的读取；目录删除先进入事务 quarantine。最终归档 marker 之前可以安全回滚，写入 marker 后只能 exactly-once continue。

## 状态检查、自动推进与恢复

每次恢复从磁盘事实开始：先读配置，再运行 `comet native status` 和 `show`，然后读取目标 change、canonical spec、实现和测试。不要依赖聊天记录猜阶段，也不要手改 phase、Run state、锁或 transaction journal。

阶段内可以用 `checkpoint` 保存摘要、下一动作和内容寻址的产物 manifest。`status` 返回最新 checkpoint 和结构化 continuation：`continue-same-skill` 表示继续当前 Skill，`await-user` 表示等待真实决定，`repair-runtime` 表示先恢复 Runtime，`stop` 表示完成或停止。它让新的会话能恢复工作，但不代表后台 daemon 会自行调用模型。

`list`、项目级 `status`、详细 findings 和 Acceptance ID 都使用有界分页。Cursor 绑定当前名称集合或 acceptance hash；输入变化后旧 cursor 会明确失效，不会静默错页。输入和输出超限时 Runtime 失败关闭，不通过截断需求正文掩盖遗漏。

`comet native next` 在阶段条件满足时自动推进；条件不足时返回结构化检查结果并停在当前阶段。普通推进通过 `runtime/transition.json` 先记录确定的前后状态，再依次写 Run state、`change.yaml`、trajectory 和 checkpoint；中断后下一次 `next` / `archive` 会自动继续，也可用 `doctor --repair --strategy continue` 显式恢复。

`comet native doctor` 默认只读，`--repair` 只处理可证明安全的陈旧 selection、锁和确定性事务恢复，不会自动重写用户编写的 brief、规格或验证报告。普通推进不支持 rollback；Archive 和 root move 仍按各自 journal 的安全阶段决定能否回滚。

Native 还提供一个窄范围的 `comet native check`：它只在 Verify 对当前 implementation scope 做内置、有界的只读文本检查，并产生内容寻址 receipt。它不执行 Git、shell、项目脚本或外部 Skill。真实项目测试仍由强模型按任务风险选择和运行。

`comet status` 会把 Native、Classic 和未托管 OpenSpec change 分区展示。自然语言恢复由 `comet resume-probe` 先解析项目默认工作流，再只检查对应一侧；Native 恢复永远进入 `/comet-native`，不会把 `comet native next` 误当作恢复命令，也不会扫描 Classic 目录。配置损坏时探针会停止并要求修复，而不是猜测回退。

完整命令见 [Native CLI](/zh/cli/native)。

## Dashboard 只是观察面

Dashboard 可以展示 Native change 的阶段、证据新鲜度、continuation、checkpoint、归档就绪和冲突摘要，但它只消费 Runtime 的只读 projection。它不重新推导状态、不缓存一份新的事实，也不提供推进或修复按钮。CLI/Runtime 始终是唯一写入方。
