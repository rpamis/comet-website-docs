---
title: 'Native 工作流'
description: '面向 Fable 5、GPT-5.6 及同档强模型的自包含需求工作流：需求具体、方法轻量、验收可恢复。'
---

Native 面向 Fable 5、GPT-5.6 及同档强模型。这类模型能自主调查代码、形成实现方案并按风险选择测试，因此 Native 不强制固定的设计、计划、TDD 或 review 方法；但完成判定不交给模型自报。Runtime 运行必要检查、分派新的只读 Verifier，并保存可跨设备恢复的用户状态。

## Native Loop

```mermaid
flowchart LR
  S["Shape：目标、决定、Specs"] --> B["Build：实现与 handoff"]
  B --> C["Runtime checks"] --> V["Verify：新 Verifier"]
  V -->|失败| B
  V -->|通过| A["Archive：应用 Specs 并归档"]
```

每轮都覆盖完整 A1…An。Builder 的“完成”只是交接信息；漏项、重复项、未知项或检查失败都会返回 Build。相同候选下已完成的检查会复用，Archive 正常路径不再重复验收。

## 四阶段

### Shape：固定目标与用户决定

Shape 调查仓库事实，写 `brief.md`、完整 target Specs 和用户可确认的共享理解。改变用户可见结果、默认行为、兼容性、范围、风险或不可逆结果的问题交给用户；实现结构和库选择由模型自主决定。新项目默认 Batch，Sequential 仍可配置。

### Build：模型自主实现

Build 以 brief、Specs 和仓库规则为边界。模型自行选择实现、测试和修复方式；完成后提交 Builder handoff，不直接写通过结论。出现新的产品决定时，回 Shape；实现缺口则在下一轮 Build 修复。

### Verify：Runtime 检查 + 新 Verifier

Runtime 解析必要检查并执行，stdout/stderr 流入本机日志，状态保留摘要。新的只读 Verifier 必须逐项给出 A1…An 结果；需要额外检查时，在当前 attempt 通过 `request-checks` 请求 Runtime。没有独立 Verifier execution 时，Runtime 明确标记降级，并在 Archive 前等待用户确认。

### Archive：只应用最终结果

Verify 通过后，Runtime 写入 `verification.md`，按 Spec 更新 canonical 文档并移动 change。正常 Archive 不重复运行检查。归档后的用户产物是 brief、Specs、`comet-state.yaml` 和 `verification.md`；本机 `.comet/runtime/native/` 执行目录会清理。

## 状态和恢复

`comet-state.yaml` 保存阶段、Loop、验收、handoff、检查摘要、Verifier 结论、阻塞、有限 history 和下一步；本机 `state.json`、日志、锁和事务位于 `.comet/runtime/native/`。换设备或缺少 overlay 时，从 YAML 重建；代码、artifact root 或 branch/worktree 不匹配则等待用户。

## Native 与 Classic

| 维度      | Native                                 | Classic                                          |
| --------- | -------------------------------------- | ------------------------------------------------ |
| 主要适用模型 | Fable 5、GPT-5.6 及同档强模型 | 低于这一能力档、需要更细阶段指导的模型 |
| 工作流    | Shape → Build → Verify → Archive       | open → design → build → verify → archive         |
| 状态/产物 | Comet brief、完整 Specs、portable YAML | OpenSpec change、`.comet.yaml`、Superpowers 产物 |
| 方法      | 模型按仓库事实和风险自主选择           | 显式设计、计划、TDD、调试和 review               |
| 依赖      | Comet Native Runtime，不依赖外部 Skill | OpenSpec 与 Superpowers                          |

`/comet` 只按 `.comet/config.yaml` 确定性转发；两套工作流的 change、Guard、状态和目录互不转换。

继续阅读：[Native 快速开始](/zh/native/quickstart)、[Native Loop](/zh/native/native-loop)、[产物与状态](/zh/native/artifacts-and-state) 和 [安全与恢复](/zh/native/safety-and-recovery)。
