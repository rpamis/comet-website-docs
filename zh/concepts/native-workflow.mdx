---
title: 'Native 工作流'
description: '面向强模型的独立需求工作流：四个阶段、一个入口、文件化状态，完成判定不靠模型自报。'
---

Native 是 Comet 面向强模型的需求工作流。Fable 5、GPT-5.6 这一档的模型能自己调查代码、形成方案、按风险挑测试，再给它们套固定的设计、计划和 TDD 流程，收益已经不大。Native 因此只管两件事：需求用什么固定下来，完成由谁判定。brief 和完整目标规格写清"做什么"，Runtime 检查加独立 Verifier 判定"算不算做完"；计划、实现、测试和审查方法留给模型自己选。

这个取舍有实测数据支撑。与 Classic 的对齐评估（16 个任务、48 次运行，取双方均通过的 41 组配对样本）里，Native 的总 token 少 76.8%，Agent 轮次少 57.4%，耗时短 47.4%，三次全通过率 87.5%。实验设计和原始数字见 [Native 与 0.4.0 Classic 真实评估](/zh/eval/comet-native-vs-040-experiment)。

## 一个 change 的生命周期

```mermaid
flowchart LR
  S["Shape：目标、决定、Specs"] --> B["Build：实现与 handoff"]
  B --> C["Runtime checks"] --> V["Verify：新 Verifier"]
  V -->|失败| B
  V -->|通过| A["Archive：应用 Specs 并归档"]
```

每一轮验收都覆盖完整的 A1…An 列表。Build 提交的"完成"只是交接信息：漏项、重复项、未知项或检查失败，一律打回 Build 重来。同一候选已经跑过的检查会复用，Archive 的正常路径不再重复验收。

## 四个阶段

### Shape：固定目标与用户决定

Shape 先调查仓库里能确定的事实，再写 `brief.md`、完整 target Specs 和一份用户可确认的共享理解。会改变用户可见结果、默认行为、兼容性、范围、风险或不可逆结果的问题，交给用户；实现结构和库的选择，模型自己定。新项目默认 Batch 澄清，Sequential 仍可配置。

### Build：模型自主实现

Build 以 brief、Specs 和仓库规则为边界。模型自己选择实现、测试和修复方式，完成后提交 Builder handoff，不写通过结论。过程中冒出新的产品决定，回 Shape；实现缺口，留在下一轮 Build 修。

### Verify：Runtime 检查 + 新 Verifier

Runtime 解析并执行必要检查，命令输出进本机日志，状态里只留摘要。新的只读 Verifier 逐项给出 A1…An 的结果；需要额外检查时，在当前 attempt 内向 Runtime 申请。宿主提供不了独立 Verifier execution 时，Runtime 会明确标记降级，并在 Archive 前等你确认。

### Archive：只应用最终结果

Verify 通过后，Runtime 生成 `verification.md`，按 Spec 更新 canonical 文档并移动 change，不重复跑检查。归档后留下的用户产物是 brief、Specs、`comet-state.yaml` 和 `verification.md`；本机 `.comet/runtime/native/` 下的执行目录会被清理。

## 状态与恢复

`comet-state.yaml` 保存阶段、Loop、验收、handoff、检查摘要、Verifier 结论、阻塞、有限的 history 和下一步；`state.json`、日志、锁和事务这些本机文件都在 `.comet/runtime/native/`。换设备或本机 overlay 缺失时从 YAML 重建；代码、artifact root 或 branch/worktree 对不上，就停下来等你。

## 和 Classic 的关系

Native 和 Classic 是两套独立的工作流，不是轻重档位，也不会互相升级，怎么选见 [Native 与 Classic 如何选择](/zh/native/native-vs-classic)。`/comet` 只按 `.comet/config.yaml` 确定性转发，两边的 change、Guard、状态和目录互不转换。

继续阅读：[Native 快速开始](/zh/native/quickstart)、[Native Loop](/zh/native/native-loop)、[产物与状态](/zh/native/artifacts-and-state) 和 [安全与恢复](/zh/native/safety-and-recovery)。
