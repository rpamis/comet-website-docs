---
title: "支持的平台"
description: "查看 Comet 支持的全部 31 个 AI 编码平台、每个平台的项目级和全局安装目录，以及各自支持的 Skill、规则和 hooks 能力。"
---

Comet 会把 Skill、规则和必要 hooks 安装到目标 AI 编码平台的约定目录。你可以在 `comet init` 里选择一个或多个平台。0.4.0-beta.1 起 Comet 支持 **31 个** AI 编码平台。

## 全部平台一览

下表列出所有平台、平台 id（用于 CLI `--platform <id>`）、项目级 Skill 目录、全局 Skill 目录，以及各自支持的能力。

### 完整能力（Skill + 规则 + hooks）

| 平台 | 平台 id | 项目目录 | 全局目录 | 规则格式 | Hooks | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| Claude Code | `claude` | `.claude` | `.claude` | `md` | ✅ `claude-code` | 参考平台，支持 PreToolUse hooks |
| Codex | `codex` | `.codex` | `.codex` | `md` | ✅ `claude-code` | 独立 CODEX_HOME 插件缓存检测 |
| Amazon Q Developer | `amazon-q` | `.amazonq` | `.amazonq` | `md` | ✅ `claude-code` | 复用 claude-code hook 格式 |
| Windsurf | `windsurf` | `.windsurf` | `.windsurf` | `md` | ✅ `windsurf` | 写入 `pre_write_code` hooks |
| GitHub Copilot | `github-copilot` | `.github` | `.github` | `copilot` | ✅ `copilot` | 用 `.github/instructions/*.instructions.md` |
| Gemini CLI | `gemini` | `.gemini` | `.gemini` | 无（GEMINI.md） | ✅ `gemini` | 唯一无规则目录但支持 hooks 的平台 |
| Qwen Code | `qwen` | `.qwen` | `.qwen` | `md` | ✅ `qwen` | |
| Qoder | `qoder` | `.qoder` | `.qoder` | `md` | ✅ `qoder` | 与 qwen 共享 PreToolUse 处理 |
| Kiro | `kiro` | `.kiro` | `.kiro` | `md`（steering） | ✅ `kiro` | 规则在 `.kiro/steering/`，hook 是独立 `.kiro.hook` 文件 |

### Skill + 规则（无 hooks）

| 平台 | 平台 id | 项目目录 | 全局目录 | 规则格式 | 说明 |
| --- | --- | --- | --- | --- | --- |
| Cursor | `cursor` | `.cursor` | `.cursor` | `mdc` | 唯一使用 Cursor MDC frontmatter 格式的平台 |
| OpenCode | `opencode` | `.opencode` | `~/.config/opencode` | `md` | OpenCode 风格 slash 命令，全局目录与项目不同 |
| Cline | `cline` | `.cline` | `.cline` | `md` | 规则在项目根的 `.clinerules/`，不在 `.cline/` 内 |
| RooCode | `roocode` | `.roo` | `.roo` | `md` | |
| Continue | `continue` | `.continue` | `.continue` | `md` | |
| Kilo Code | `kilocode` | `.kilocode` | `.kilocode` | `md` | |
| Auggie | `auggie` | `.augment` | `.augment` | `md` | Augment CLI |
| Lingma | `lingma` | `.lingma` | `.lingma` | `md` | |
| Trae | `trae` | `.trae` | `.trae` | `md` | |
| ZCode | `zcode` | `.zcode` | `.zcode` | `md` | 基于 OpenCode，会把 OpenSpec opencode 输出镜像到 `.zcode/` |
| MimoCode | `mimocode` | `.mimocode` | `~/.config/mimocode` | `md` | 基于 OpenCode，OpenCode 风格 slash 命令 + 镜像 OpenSpec 输出 |

### 仅 Skill（无规则、无 hooks）

| 平台 | 平台 id | 项目目录 | 全局目录 | 说明 |
| --- | --- | --- | --- | --- |
| Kimi Code | `kimicode` | `.kimi-code` | `.kimi-code` | OpenSpec 工具 id 为 `kimi` |
| Junie | `junie` | `.junie` | 无全局目录 | |
| CodeBuddy Code | `codebuddy` | `.codebuddy` | 无全局目录 | |
| CoStrict | `costrict` | `.cospec` | 无全局目录 | 目录名与 id 不同 |
| Crush | `crush` | `.crush` | 无全局目录 | |
| Factory Droid | `factory` | `.factory` | 无全局目录 | |
| iFlow | `iflow` | `.iflow` | 无全局目录 | |
| Pi | `pi` | `.pi` | `~/.pi/agent` | 全局目录是子目录 |
| Antigravity | `antigravity` | `.agents` | `~/.gemini/antigravity` | 项目和全局目录完全不同，支持 CodeGraph |
| Bob Shell | `bob` | `.bob` | 无全局目录 | |
| ForgeCode | `forgecode` | `.forge` | 无全局目录 | 目录名与 id 不同 |

## 能力汇总

### 规则格式

Comet 支持三种规则格式：

| 格式 | 使用平台 |
| --- | --- |
| `md` | 绝大多数支持规则的平台 |
| `mdc` | 仅 Cursor（Cursor MDC frontmatter） |
| `copilot` | 仅 GitHub Copilot |

### Hooks 格式

支持 hooks 的 9 个平台使用 7 种 hook 格式：

| Hook 格式 | 写入文件 | 使用平台 |
| --- | --- | --- |
| `claude-code` | `settings.local.json` | Claude Code、Codex、Amazon Q |
| `qwen` | `settings.json` | Qwen Code |
| `qoder` | `settings.json` | Qoder |
| `gemini` | `settings.json` | Gemini CLI |
| `windsurf` | `hooks.json` | Windsurf |
| `copilot` | `hooks/<id>.json` | GitHub Copilot |
| `kiro` | `hooks/<id>.kiro.hook` | Kiro |

### OpenCode 系平台

OpenCode、ZCode、MimoCode 都基于 OpenCode 运行时，支持 OpenCode 风格 slash 命令和镜像 OpenSpec 输出：

| 平台 | 镜像行为 |
| --- | --- |
| OpenCode | 原生支持 |
| ZCode | 把 `.opencode/{skills,commands}` 迁移镜像到 `.zcode/` |
| MimoCode | 镜像 OpenSpec 输出到 `.mimocode/` |

## 特殊安装路径

以下平台的全局安装目录与项目级目录不同，安装时需要注意：

| 平台 | 项目级目录 | 全局目录 |
| --- | --- | --- |
| OpenCode | `.opencode` | `~/.config/opencode` |
| MimoCode | `.mimocode` | `~/.config/mimocode` |
| Antigravity | `.agents` | `~/.gemini/antigravity` |
| Pi | `.pi` | `~/.pi/agent` |

其他平台的全局安装使用同名目录（如 Claude Code 全局为 `~/.claude`）。Junie、CodeBuddy Code、CoStrict、Crush、Factory Droid、iFlow、Bob Shell、ForgeCode 不支持全局安装。

<Tip>
  `comet init` 会自动检测项目里已存在的平台配置目录并预选。你也可以用 `--yes` 跳过交互，自动安装到所有检测到的平台。
</Tip>

## 项目级还是全局

| 安装范围 | 适合场景 | 特点 |
| --- | --- | --- |
| project | 团队项目、仓库约定、需要提交配置 | 配置写入当前项目，便于协作 |
| global | 个人默认环境、多个项目共享 | 配置写入用户目录，切项目更省事 |

<Tip>
  如果你不确定，先用项目级安装。它最容易让团队成员看到同一套 Comet 行为。
</Tip>

## 通用 skills 注册表平台

有些 AI 编码平台使用通用 `skills` CLI 注册表，而不是平台专属目录约定。对这些平台（包括 OpenClaw、Hermes 等），直接用通用命令安装 Comet skill 包：

```bash
npx skills add rpamis/comet
```

这条命令从通用 skills 注册表拉取 Comet skill 包并安装到平台期望的位置，不需要全局 Comet 安装。它和 `comet init` 可以并存，互不冲突。

## hooks 和可执行能力

部分平台支持 hooks。Comet 会非破坏性合并自己管理的 hook 配置，保留用户已有配置。

`/comet-any` 分发用户生成的 Skill 时，如果目标 Bundle 含 hook 或脚本，`comet publish distribute` 会要求确认 executable disclosure。不要在没有理解脚本副作用时强行分发。

## 安装后验证

```bash
comet doctor
```

如果平台目录、Skill 文件、脚本或 hooks 缺失，`doctor` 会给出诊断。需要更新时运行：

```bash
comet update
```
