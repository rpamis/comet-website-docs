---
title: "支持的平台"
description: "查看 Comet 可安装到哪些 AI 编码平台，以及项目级和全局安装如何选择。"
---

Comet 会把 Skill、规则和必要 hooks 安装到目标 AI 编码平台的约定目录。你可以在 `comet init` 里选择一个或多个平台。

## 常用平台

| 平台 | 平台 id | 说明 |
| --- | --- | --- |
| Claude Code | `claude` | 支持 Skill、规则和 PreToolUse hooks |
| Codex | `codex` | 支持 Skill、规则和 hooks |
| Cursor | `cursor` | 支持项目规则和 Skill 文件 |
| OpenCode | `opencode` | 支持 OpenCode 风格配置 |
| Gemini CLI | `gemini` | 使用 Gemini 规则文件和 hook 配置 |
| GitHub Copilot | `github-copilot` | 支持 Copilot 指令与 hooks |
| Qwen Code | `qwen` | 支持 Qwen 风格配置 |
| Kimi Code | `kimicode` | 支持 Kimi Code 目录 |
| ZCode | `zcode` | 基于 OpenCode 的新增平台 |

Comet 还包含 Windsurf、Cline、RooCode、Continue、Amazon Q Developer、Kilo Code、Auggie、Kiro、Lingma、Junie、CodeBuddy Code、CoStrict、Crush、Factory Droid、iFlow、Pi、Qoder、Antigravity、Bob Shell、ForgeCode、Trae 等平台适配。

## 项目级还是全局

| 安装范围 | 适合场景 | 特点 |
| --- | --- | --- |
| project | 团队项目、仓库约定、需要提交配置 | 配置写入当前项目，便于协作 |
| global | 个人默认环境、多个项目共享 | 配置写入用户目录，切项目更省事 |

<Tip>
  如果你不确定，先用项目级安装。它最容易让团队成员看到同一套 Comet 行为。
</Tip>

## hooks 和可执行能力

部分平台支持 hooks。Comet 会非破坏性合并自己管理的 hook 配置，保留用户已有配置。

`/comet-any` 分发用户生成的 Skill 时，如果目标 Bundle 含 hook 或脚本，`comet publish distribute` 会要求确认 executable disclosure。不要在没有理解脚本副作用时强行分发。

## 安装后验证

```bash
comet doctor
```

如果平台目录、Skill 文件、脚本或 hooks 缺失，`doctor` 会给出诊断。需要更新时运行：

```bash
comet update
```
