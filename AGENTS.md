> **First-time setup**: Customize this file for your project. Prompt the user to customize this file for their project.
> For Mintlify product knowledge (components, configuration, writing standards),
> install the Mintlify skill: `npx skills add https://mintlify.com/docs`

# Documentation project instructions

## About this project

- This is a documentation site built on [Mintlify](https://mintlify.com)
- Pages are MDX files with YAML frontmatter
- Configuration lives in `docs.json`
- Use the Mintlify MCP server, `https://mcp.mintlify.com`, to edit content and settings via MCP
- Use the Mintlify docs MCP server, `https://www.mintlify.com/docs/mcp`, to query information about using Mintlify via MCP

## Terminology

{/* Add product-specific terms and preferred usage */}
{/* Example: Use "workspace" not "project", "member" not "user" */}

## Style preferences

{/* Add any project-specific style rules below */}

- Use active voice and second person ("you")
- Keep sentences concise — one idea per sentence
- Use sentence case for headings
- Bold for UI elements: Click **Settings**
- Code formatting for file names, commands, paths, and code references

### 中文技术文档写作

- 本站是 Comet 面向用户的产品文档。所有面向网站发布的中文内容都应达到可直接发布的产品文档标准；不要把发布正文写成内部设计记录、开发过程复盘、PR 总结或研究备忘录。
- 以用户任务和用户可感知的产品行为组织内容：用户在什么场景使用、需要做什么、会看到什么结果、有哪些限制。只有解释使用方法、行为边界或故障排查所必需时，才展开内部实现。
- 使用产品中的正式名称和用户可见术语。不要把源码标识、内部缩写或团队黑话直接当成面向用户的表达；确需保留时，在首次出现处解释。
- 使用中性、专业、自然的现代汉语。句子应符合中文语序和表达习惯，指代明确、衔接顺畅、读起来通顺；避免英文直译腔、抽象名词串、过度书面化、公文套话和口号式表达。
- 产品化不等于营销化。可以说明价值和收益，但必须落实到具体场景、产品行为、数据或证据。
- 开头直接说明对象、行为、适用条件或结果。不要用行业趋势、产品愿景、价值判断或“重要性”铺垫。
- 每段只完成一个任务，每句话只表达一个意思。优先写清主体、动作、对象、条件和结果；一句话包含多个转折或操作时，拆成短句。
- 每段必须增加事实、操作、条件、限制、原因或结果。删除不影响理解、判断或操作的导语、转场和结尾。
- 使用项目已有术语。一个概念固定使用一个名称，不为避免重复而替换同义词。
- 说明功能时，写出真实行为、输入、输出、状态或边界。产品能力、性能和效果必须有当前源码、测试、数据或明确条件支撑。
- 内容讲完即停止。不要自动添加总结、展望、价值升华、宣传性结尾，或换一种说法重复结论。
- 列表只用于真实并列、比较、排序或步骤。不要为了显得完整强行凑成三点，也不要让连续段落使用相同句式。
- 标题直接概括用户任务或页面对象。避免口号、设问、双标题和抽象价值判断。
- 粗体只用于 UI 元素、需要定义的术语和确有必要的警告。不要靠连续加粗短语制造语势。
- 改写或润色已有内容时，保留原文的加粗、引号等强调标记，只改文字表达；只有当强调本身在给空洞结论制造语势时才连标记一起删，拿不准就保留。
- 改写或翻译时，先保留原文事实、范围、体裁、语气和结构。技术博客保留作者的第一人称表达，不要统一改写成中性的 AI 总结。

### 文档结构与去重

- **单一权威页**：同一事实（停顿点清单、恢复矩阵、发布状态链、审查归属、命令定义等）只在一页完整写，其余页面引用权威页并注明"见 X"。新增内容先想清楚它属于哪个权威页。
- **跨页编号全局唯一**：停顿点编号、步骤编号、选项编号不得在不同页面自相矛盾；改动一处时 `git grep` 全站同步其它页。
- **决策先行、机制后置**：概念/阶段页先回答"用户在哪个时刻会被问什么、选什么、看到什么、有什么代价"，协议字段、内部步骤、文件结构放页面后段或收进折叠，并标注"想深入/排障再读"。
- **页面只讲本页职责**：指南页做教程、命令页做命令参考、原理页做机制——不要让同一段机制在两三种页面里各写一遍；与邻居页重复时，保留权威版本并把其它页改成链接。
- **全局替换防叠字**：用脚本/批量替换改词时，替换串不得包含被替换串（如"预检"→"预检查"会把已有"预检查"连锁成"预检查查"）。替换后 grep 目标词复查全站有无叠字或误伤。
- **home.mdx 是手工打磨页**：首页是营销门面，行业术语与卖点表述保持原样；批量改写、去 AI 味或统一措辞前先与用户确认，不把首页当普通文档批量处理。

### 模板化表达复核

- 没有证据时，不得写“显著提升”“大幅降低”“完全解决”“零成本”“最佳”“无缝”等效果或承诺。
- 以下表达只作为复核信号，不机械禁用；单独出现一次通常不改，反复出现、脱离事实或让句子空转时才处理：
  - 句式：`不是……而是……`、`不仅……更/还/也……`、`从……到……`、`随着……`、`在……背景下`（无信息时）、`值得一提/不可否认`、`看似……本质/背后……`、`首先/其次/最后`（非真实步骤时）、`这意味着/这说明/由此可见`（连续出现时）、`综上所述/总而言之`、`尽管……仍值得期待`
  - 词：赋能、助力、打造、闭环、抓手、沉淀、生态、布局（无具体动作或对象时）；相关领域、广泛影响、各类场景、某种意义（泛化无细节时）；研究表明、普遍认为、业内人士（无出处时）；可能、或许、通常、相对、一定程度上（同一句连续叠加时）
- 命中复核信号时，依次检查：这句话是否新增事实；评价是否有证据；删掉句模后是否更直接。没有信息增量或证据时，改成具体动作、对象、条件、数据或结果。
- 安全边界、禁止行为、失败条件、配置状态、术语辨析、真实对比和原文引用可以保留必要的否定或复核词。不要为规避词表损害准确性。

### AI 味与直译防护

- **先分清两类英文词，处理方式相反：**
  - **行业通用术语保留原文，不翻译、不删除、不强行白话化**：HITL、DAG、ReAct、Runtime、Harness、Rubric、Pass@k、Loop Engineering、Spec、TDD、BDD、PR、CI/CD、Token、Prompt、Agent、Skill、Workflow 等。目标读者（用 AI 编码工具的开发者）认识这些词，删掉反而显得不专业。可以保留原文直接使用，或必要时在首次出现处补一个简短中文说明，但术语本身必须保留。
  - **Comet 内部自造词和源码标识符才需要翻译或解释**：machine-owned、tripwire、readiness、lane、verbatim 投影这类产品内部概念，按下方对照表处理；`.ts` 文件名、函数名、类型名不出现在面向用户的正文。

- 常见英文概念按下表翻译，不直译；表外新词先想“中文产品文档会怎么写”再落笔：
  - surface → 入口；source of truth → 唯一可信来源；escape hatch → 兜底开关 / 备用通道；prose → 纯文字；first-class → 正式（如“正式产物”）；bounded → 有限；leaky → 不生效 / 漏过；bucket → 类；mental model → 预期 / 工作方式；noise → 环境噪声；contract（技术接口语义）→ 契约
  - ground truth → 真实状态（首次出现可附英文）；制品 / artifacts（正文语境）→ 产物；fail closed → 失败关闭（首次出现附英文注释）
  - machine-owned → “由 Comet 自动维护”（首次出现附英文）；tripwire → 文件数阈值；stale pause → 残留的暂停标记；dirty worktree → 未提交改动（首次出现附英文）；verbatim → 原样写入 / 完整原文；conflict radar → 冲突检测
  - portable 状态 → 可跨设备同步的状态；canonical Spec → 主 Spec；readiness → 发布就绪状态；lane / authoring lane → 创作通道；harness → 评测框架（eval 语境）；treatment → 实验条件；child → 子任务 / 子变更；analysis set → 进入统计的运行样本
  - projection / 工作流投影 → 工作流状态；spec 投影 / structured projection → spec 摘录 / 结构化摘录；read model / 读模型 → 索引；stable boundary / trusted state / 可信状态 / 稳定边界 → 记录的进度 / 状态；semantic identity / 内容身份 → 按内容判断是否同一记录
  - 事务化 → 事务式 / 以事务方式；活文档（living document）→ 持续维护的文档；多票评审 → 多轮投票评审；语义归纳 → 归纳总结；路径逃逸 → 路径穿越；常驻上下文 → 注入的上下文；勾选验证 → 核验后勾选；协调会话 → 主会话；恢复锚点 → 恢复检查点 / 恢复依据；原子地（atomically）→ 一次性（并说明要么全部生效、要么不执行）；自愈 → 自动修正；分布性 → 跨多次运行统计；诊断性 → 诊断参考；硬判定 → 硬性判定
- 源码标识符（`.ts` 文件名、函数名、类型名，如 `CometIntentFrame`、`resolveSkill`、`reviewBundle`）不出现在面向用户的正文；确需引用时先说明它承担的作用，再附标识符。进阶页也遵守“先中文作用、后英文标识”。
- “确定性”只用于固定搭配（确定性测试、确定性验证、确定性保证、确定性状态机）；其余场景改写为“固定”“自动”“可复现”“有明确规则”。
- “授权”只用于权限语境；流程中的确认动作写“确认”“批准”。归档场景统一用“归档批准”，不写“归档授权”。
- 以下句式出现即复查，多数应改写：`作为……（存在/能力）`、单个词加粗独立成句（如“**信息性**”）、`0 上下文`（写“零上下文”或“无上下文”）。
- 格式基线：中文与英文、数字之间加空格；正文用全角标点和中文弯引号；代码、命令、路径除外。
- 编号列表必须连续且从 1 开始；表格和正文句末不要漏句号（列表项按上下文判断）。

### 翻译腔与欧化中文防护

从英文翻译或改写时以信达雅为准：信是事实与范围不增不减；达是符合中文语序、通顺可读；雅是干净克制、不用虚饰词凑语气。方法上先读懂整段，盖住原文用中文重说一遍，再核对事实，不逐句对译。

- 少用被字句。中文常用主动形式表被动：“文档已更新”，不写“文档被更新”；只有需要强调受事或责任时才用“被”。
- 动词能直接用就不用“万能动词＋抽象名词”：“进行研究”→“研究”，“作出贡献”→“贡献了”。
- 连续两个以上“的”的定中结构要拆；更不能让多层定语把主干压到句尾（倒装感）。
- 不加无信息量的“一＋量词”：“创建一个 change”→“创建 change”。
- 状语不堆在句首：“在……背景下，通过……的方式，对……进行了……”拆成短句，先说主语和动作。
- 见 when/if 不必然译“当……的时候”；多数场合直接写条件或时间。
- “性 / 化 / 度”词缀不叠用：“可配置性很高”→“配置项很多”。
- 不写“最……之一”（one of the best 的直译），直接给事实。
- 破折号、分号、冒号不在一句内反复承担句法关系；出现两次以上时拆句。
- GPT 式翻译腔骨架，命中即复查（判断流程同“模板化表达复核”）：
  - 用物理动作直译思考或系统行为：接住、击穿、锋利、收口、打穿、扛住、撑不住。中文日常说“收到、处理、解决、完成、支持不住”。
  - 形容词先下判断再用冒号引出内容：“更锋利的重构：”“逻辑很清晰：”。删掉判断，或改写成事实。
  - 抽象名词做主语下结论：“X 的 Y 比 Z 更 W”骨架，改成具体对象和动作做主语。
- 有稳定中文译名的词不夹英文：context→上下文、state→状态、cache→缓存、claim→断言；术语未收敛的行业词按“AI 味与直译防护”的规则保留原文。
- 正文不用反问句推进说明（“这不正是……吗”“何乐而不为”）；设问只用于自问自答场景（FAQ 标题、排查引导），且必须紧跟答案。
- 自查方法：写完一段圈出所有动词，中文日常不这样搭配的就换；再只读各句主干，主干被修饰语压住的拆句。

### 亮点与强调保留（改写红线）

- 改写或润色时必须保留原文的事实性卖点：实验数据、对比结论、已验证的优势、适用边界。只改表达，不删信息。
- 删掉无证据的最高级（“最受欢迎”“最佳”“颠覆”）时，必须补上具体收益（解决了什么问题、行为变化、数字），不能只删不补。
- 有证据的优势表述和宣传语允许保留，不要为了“去 AI 味”把对比、卖点句改平。
- 技术博客只校对错别字、标点、编号和指代，不改文风、不删作者原话。
- 交稿前用 `git diff` 自查：被删行中的加粗短语和卖点句，要么在新增行中有对应保留，要么有明确替换理由。

### 中文文风对照

反例：

> Comet 不只是一个工作流工具，更是连接想法与可靠交付的强大闭环，让你轻松完成复杂变更。

正例：

> Comet 把需求、实现和验收记录保存在同一个 change 中。运行 `comet status` 可以查看当前阶段和下一步命令。

反例：

> Native Loop 不是让模型无限自我反思，而是一套输入明确、证据完整、预算有限、知道何时停止的工程流程。

正例：

> Native Loop 为每轮实现设定验收范围、检查预算和退出条件。

反例（翻译腔）：

> 更锋利的验证：Comet 通过对验收项的逐项核验，实现了对交付质量的深度把控。

正例：

> Comet 在 verify 阶段逐项核验验收项，全部通过后 change 才能归档。

### 中文内容交付前检查

按顺序执行：

1. 核对产品名称、术语、命令、路径、版本号、数字、链接是否与站内权威页一致；正文版本指称（如"以 X 版本为准""X 版本起"）先查 changelog，不写易过期的句子。
2. 检查本次改动是否撞上"单一权威页"：若与权威页重复，改回引用并删本地重写。
3. 搜索本次修改中的复核信号（见"模板化表达复核"与"翻译腔与欧化中文防护"）并逐条判断；复核信号不是自动替换清单。
4. 用 `git diff` 自查：被删行中的加粗短语和卖点句，要么在新增行中有对应保留，要么有明确替换理由。
5. 通读全文，检查中文语序、指代和句间衔接；需要按英文结构回译才能理解的句子，改成自然中文。
6. 确认正文服务于用户理解和使用产品，没有混入只对开发者有意义的实现过程、内部结论或项目汇报口吻；与上面正反例比较，读起来像宣传稿或统一模板时改成具体事实和自然段落。
7. 若本次是结构/内容级改动（新增页面、移动章节、删除小节、行为修正），按"网站修改规范"同步英文。

## Content boundaries

- 面向用户的中文页面不写源码标识、内部实现细节和开发过程复盘；机制类内容压缩为"你会看到什么、该做什么"，或链到脚本/原理页。
- 不描述源码未实现或已被源码否定的行为；正文版本指称随 changelog 更新，不写"以某版本为准"这类易过期句子。

## 网站修改规范

- 先修改中文，用户同意后再修改英文。
- zh 做**结构/内容级改动**（新增页面、移动章节、删除小节、行为修正、术语/编号变更）后，把 en 同步成同结构、同语义的版本；纯中文措辞/文风改动不必同步 en。
- en 同步后校验：无中文残留、无 `/zh/` 链接、代码围栏成对、mermaid/标签完整；新增页面要同时加入 `docs.json` 的 zh 与 en 导航。
- en 若存在历史机翻残句或占位乱码（如坏 token、错位文本节点），在同步时顺带修正为自然英文。
