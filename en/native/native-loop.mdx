---
title: 'Native Loop principles'
description: 'Understand how Runtime checks, a fresh Verifier, and bounded counters turn self-reported completion into a recoverable acceptance loop.'
---

An ordinary Agent loop lets the model decide “am I done?”. Native Loop gives that verdict to Runtime: the model understands and implements, while Runtime runs checks, dispatches review, records gaps, and decides when to stop.

## One Loop

```text
Shape → Builder handoff → Runtime checks → fresh Verifier
                              ↑                 ↓
                         repair gaps ← fail     pass → Archive
```

Every round uses the complete A1…An acceptance list. A Builder's self-report is context, not proof. Missing, duplicate, or unknown Verifier IDs and failed checks are rejected by Runtime and become concrete input for the next Build round.

## How progress is judged

Runtime stores bounded history and counters:

- `iteration`: one implementation repair round;
- `attempt`: the current Verify attempt;
- `no_progress_count`: times the unresolved acceptance set did not strictly shrink;
- `failed_iteration_count`: implementation-failure rounds;
- `execution_failure_count`: Runtime or Verifier execution errors.

Fewer unresolved items, a failed check becoming passing, or clearly missing information being supplied counts as real progress. Rewriting explanations, rerunning the same command, or shuffling the same failures does not reset stagnation. Repeated errors or no progress at the limit returns `await-user` or `blocked`.

## Only necessary checks run

Runtime resolves and executes the required checks. A completed matching plan for the same candidate is reused; extra checks are requested through `request-checks` in the current attempt with round and duplicate budgets. stdout/stderr streams to local logs and the report keeps a summary; a long log does not invalidate the result by itself. The normal Archive path does not rerun checks.

## State and recovery

`comet-state.yaml` stores the stable Loop summary and next action, while `verification.md` is the user-readable report. Local `state.json`, logs, locks, and transactions live under `.comet/runtime/native/`. On another device, local execution is treated as interrupted and Runtime rebuilds what it needs from YAML; neither model memory nor an old local overlay is treated as completion proof.

## When Runtime stops

Runtime stops with a concrete action when:

- a product decision still needs the user;
- code, branch/worktree, or artifact root does not match;
- acceptance coverage is incomplete or a required check fails;
- the Verifier is unavailable, execution errors reach their limit, or a semantic result needs user resolution;
- parallel changes target the same capability and an archive order must be chosen.

The Loop therefore does not run indefinitely: once every acceptance item passes, the change archives; any remaining decisions and risks return to the user instead of consuming more Agent turns.

Continue with [Verification and repair](/en/native/verification-and-repair) and [Continuation](/en/native/continuation-and-checkpoints).
