---
title: 'Multiple changes and specification conflicts'
description: 'Run several Native changes in parallel, understand ownership and workspace choices, and archive them safely.'
---

A project can carry several active Native changes at once. `.comet/current-change.json` selects which workflow/change owns the next write; it does not limit active changes. Runtime serializes Archive by capability so one change cannot silently overwrite another.

## Parallel changes and working directories

When creating a change, Native checks the current directory and registered worktrees. When isolation matters, it shows the applicable `current`, `branch`, and `worktree` choices with a recommendation. If another active change already uses the current directory, `worktree` is usually the safe choice. Each change keeps its branch/worktree binding, and recovery stops in the wrong directory.

Existing changes are not moved to a new worktree automatically. Before continuing on another device, sync the code and formal Native files, then verify the artifact root and branch/worktree.

## Inspect and select

```bash
comet native status
comet native status <change-name> --details --json
comet native select <change-name>
```

`status` and `show` are read-only and do not switch the current change as a side effect. Several candidates, missing selection, or a selection for another workflow returns `await-user` for an explicit choice.

## What can proceed in parallel

Changes that affect different capabilities and share no behavioral constraints can proceed together. Before starting, check:

- whether both changes modify the same capability Spec;
- whether both claim the same user document or configuration;
- whether one acceptance plan depends on behavior the other has not archived;
- whether the current workspace contains another change's modifications.

Runtime can detect capability overlap in registered worktrees, but it is not a distributed lock across remote branches or devices. Unsynced code and worktrees still require team coordination.

## Archive order for one capability

When two changes target the same capability, Archive returns a serial-order blocker. Choose the first change, then let the other read the updated canonical Spec and, when needed, run:

```bash
comet native spec rebase <change-name> --summary <adjustment-summary>
```

Rebase aligns the target Spec with the latest behavior and requires a new Build and Verify. Do not overwrite the first change with the later files or reuse a pass from before the conflict.

## Cross-device collaboration

Sync project code, `.comet/config.yaml`, the brief, Specs, `comet-state.yaml`, and an existing `verification.md`. The local `.comet/runtime/native/` may be absent and will be rebuilt. If artifact roots or branch/worktree bindings differ between devices, recovery waits for an explicit correction.

Continue with [Artifacts and state](/en/native/artifacts-and-state), [Verification and repair](/en/native/verification-and-repair), and [Recovery and troubleshooting](/en/native/recovery-playbook).
