---
title: 'Comet: Agent Skill Harness For Turning Ideas Into Evaluated Workflows'
sidebarTitle: 'Overview'
description: "Comet offers two sets of recoverable workflows: Native for strong models and Classic with full phase governance, plus Skill creation, evaluation, and publishing. New projects default to Native; pick Classic only when you need full phase governance or already have Classic state."
---

Welcome to **Comet** (`@rpamis/comet`). Comet provides two independent, interruptible requirement workflows for AI coding tasks: Native and Classic.

## Which workflow should you pick

- **Native** is built for strong coding models. It gives you detailed requirements, a complete target specification, status checks, and verification evidence, but it does not prescribe a fixed plan, TDD, or review method.
- **Classic** builds on [OpenSpec](https://github.com/Fission-AI/OpenSpec) and [Superpowers](https://github.com/obra/superpowers), keeping the full five-phase governance for teams that want design, planning, TDD, and review on a fixed schedule.

The default workflow in the project root's `.comet/config.yaml` decides which entry `/comet` forwards to. `/comet` reads only the project configuration and never guesses the mode from the task size.

**New projects default to Native.** Only two situations call for Classic:

- You need full phase governance in the OpenSpec/Superpowers style.
- The project already has Classic state, or you are already using OpenSpec and Superpowers.

| My situation | Which workflow |
| --- | --- |
| New project, not initialized yet | **Native** (default) |
| Not sure what to pick; want to try an interruptible, resumable flow first | Native |
| Need fixed phase governance such as planning, TDD, and review | Classic |
| Project already has Classic state, or is using OpenSpec + Superpowers | Classic |

The two workflows are independent, and you can enable both in the same project (`comet init --workflow both`); each manages its own changes, and a single `/comet` call always enters exactly one of them. Defaults are fixed into the project configuration at initialization time, so an activated project is not rewritten by later changes to the global defaults. For a side-by-side comparison, see [Native or Classic](/en/native/native-vs-classic); for initialization options, see [comet init](/en/cli/init).

Full installation and your first change are covered in [Quickstart](/en/quickstart).

## Continue from here

<CardGroup cols={2}>
  <Card title="Introduction" icon="star" href="/en/introduction">
    What Comet is, what problems it solves, and how the workflows operate
  </Card>
  <Card title="Quickstart" icon="rocket" href="/en/quickstart">
    Install Comet and run your first `/comet` workflow in under a minute
  </Card>
  <Card title="Native Workflow" icon="arrows-rotate" href="/en/concepts/native-workflow">
    The lightweight Shape, Build, Verify, and Archive flow for strong models
  </Card>
  <Card title="Platforms" icon="laptop-code" href="/en/platforms">
    Comet supports 37 AI coding platforms, including Oh My Pi and DeepSeek Harness. Browse the platform catalog and install paths
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/en/cli/init">
    Usage guide and full reference for the Comet CLI
  </Card>
  <Card title="Scripts" icon="scroll" href="/en/scripts/overview">
    Guard scripts, state machine management, auto-advance mechanisms, and archiving tools
  </Card>
  <Card title="Combination Skill" icon="blackberry" href="/en/skill-creator/getting-started">
    Combine any Skill with `/comet-any` just like a Comet workflow, producing evaluable, publishable, distributable Skill Bundles
  </Card>
  <Card title="Evaluate Skill" icon="vial-circle-check" href="/en/eval/quickstart">
    Evaluate any Skill with Rubric scoring, Pass@k, and Pass^k; generate HTML reports and failure attribution, and let evaluation drive Skill evolution
  </Card>
</CardGroup>

## AI-native documentation

You can connect the Comet documentation site to your agent over MCP and look up the latest documentation during a conversation whenever you need it.

### In Claude Code

```bash
claude mcp add --transport http docs-comet --scope user https://docs.comet.rpamis.com/mcp
```

### In Codex CLI

```bash
codex mcp add docs-comet --url https://docs.comet.rpamis.com/mcp
```

### In Cursor / Antigravity

```json
{
  "mcpServers": {
    "docs-comet": {
      "url": "https://docs.comet.rpamis.com/mcp"
    }
  }
}
```

### In VS Code

```json
{
  "servers": {
    "docs-comet": {
      "url": "https://docs.comet.rpamis.com/mcp"
    }
  }
}
```
