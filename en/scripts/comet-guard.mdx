---
title: "comet-guard.mjs — phase transition guard"
sidebarTitle: "comet-guard.mjs"
description: "The guard runtime validates phase exit conditions before Comet advances workflow state."
---

`comet-guard.mjs` protects the workflow from moving forward without required evidence. It validates phase-specific conditions and reports concrete blockers.

## Guarded phases

| Phase | Exit requires |
| --- | --- |
| open | proposal, spec delta, and tasks are present. |
| design | design artifacts and handoff evidence are current. |
| build | tasks are complete, workflow fields are valid, and the detected project build check passes. |
| verify | verification report exists and branch/worktree status is handled. |
| archive | archive evidence confirms the change is closed. |

## What blocked means

A blocked result means Comet found missing or stale evidence. Fix the named artifact or check, then rerun the workflow. The guard should not partially update state when a required check fails.

## Build checks

The build phase no longer reads `build_command` or `verify_command` fields. Guard detects the project build entrypoint from project files, such as `npm run build`, Maven, or Cargo.

If a change `.comet.yaml` still contains `build_command` or `verify_command`, guard rejects those keys as unknown fields. If your team needs a fixed verification sequence, put it behind the project's normal build script, for example by making `npm run build` run the full required check.

## Relationship to state

Guard validates whether a transition is allowed. State records the transition after the guard passes.
