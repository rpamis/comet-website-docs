---
title: "Overview of Dashboard"
description: "Use the local read-only Dashboard to view the status, products, and next steps of Classic, Native, and Supervisor Changes."
---

import { DashboardWebsiteDemo } from "/snippets/dashboard-website-demo.jsx";

`comet dashboard` will launch a ** locally read-only ** browser dashboard. You can switch between Classic and Native at the same entry point to view active and archive changes, task progress, acceptance results, risks, products and Git summaries.

The following is a demonstration that can be directly operated. The demonstration is completely isolated from your project.

<div className="comet-dashboard-doc-demo" role="group" aria-label="Interactive demonstration of Comet Dashboard">
  <DashboardWebsiteDemo />
</div>

## Interface Preview

<p align="center">
  <img src="/img/dashboard-light.png" alt="Comet Dashboard Bright Color Mode" width="800" />
</p>

Project Overview and Change Workspace in <p align="center"> Bright Color Mode </p>

<p align="center">
  <img src="/img/dashboard-dark.png" alt="Comet Dashboard Dark Mode" width="800" />
</p>

<p align="center"> Dark mode </p>

<p align="center">
  <img src="/img/dashboard-preview.png" alt="Comet Dashboard Markdown Product Preview" width="800" />
</p>

<p align="center">Markdown Product Preview </p>

<p align="center">
  <img src="/img/dashboard-multi-changes.png" alt="Comet Dashboard Supervisor Change" width="800" />
</p>

<p align="center">Supervisor Change and sub-changE</p>

## Start

Run in the project root directory:

```bash
comet dashboard
```

Dashboard starts and opens the browser by default at `http://localhost:4321`. Use `--port < port >` to modify the port,    and use `--no-open` to only start the service.

<Info>
The service is only bound to <code>127.0.1</code>, accessible only to the local machine, and the project file is kept read-only.
</Info>

## "Classic and Native"

The **Classic/Native** switch in the upper left corner determines the current workspace. The two views share Change Explorer, Detail area, status sidebar and product preview, and the contents of each remain displayed separately.

|                  | Classic                                      | Native                                              |
| ---------------- | -------------------------------------------- | --------------------------------------------------- |
|Life cycle| open → design → build → verify → archive     | Shape → Build → Verify → Archive                    |
|Main information|Tasks, products, Verify, risks and Git|Loop, acceptance, inspection, blocking, recovery and user products|
|Activity and Archiving|Support|Support|
|Modify or advance Change|Not supported|Not supported|

After selecting a Change, you can view the current stage, task or acceptance progress, next steps, risks and related products. The complete diagnosis is still based on `comet status` or `comet native status --details`.

## Supervisor Change and Git worktree

The Dashboard will scan the Git worktrees that have been registered in the current repository and retain the branch or directory labels of each workspace.

The Native Supervisor Change will display the completion progress of the child Change. After expanding the parent Change, you can select a certain child to view its stage, workspace, dependencies, and acceptance status. The Dashboard is only responsible for discovery and presentation. The creation of worktree, task advancement and verification execution are handled by Native Runtime.

## Product Preview

Clicking on a generated product will open the Markdown preview drawer. Preview support

- Title, list, table, task list and code highlighting;
- "Mermaid diagram;
- Structured tables of `.comet.yaml`, handoff and checkpoint;
- User-readable products of Classic and Native.

Content exceeding 256 KiB will be truncated and marked. The product content will be cleaned before rendering and cannot be executed through HTML or Mermaid scripts.

## Personal Memory and Project Knowledge

The Dashboard separates current records from history. On a Project Knowledge source page, you can see whether a file has produced a knowledge record; a searchable source with no learned record is identified separately. Expand a related record to view its title, version, state, summary, conclusions, relationships, and source references instead of only the file title.

## Theme, refresh and demonstration data

- Use the top toolbar to switch between light and dark themes; Select to save in the current browser.
- The Dashboard automatically refreshes every 30 seconds, or you can click on the toolbar to refresh it immediately.
- Adding "`?demo`" after the address can load demonstration data without reading the actual Change

```text
http://localhost:4321/?demo
```

## Use boundaries

The Dashboard remains read-only and hides the absolute path, raw logs, and native Runtime files. The promotion, repair, verification, archiving and other status changes of Change are accomplished by `/comet`, `/comet-native` and the corresponding CLI.

If the collection of a certain workflow fails, the Dashboard will truthfully display the error while continuing to show another workflow.

## Next step

- [comet dashboard command ](/en/cli/dashboard) - Complete command options
- [comet status](/en/cli/status) - View active changes in the terminal
- [Native Workflow ](/en/concepts/native-workflow) - Understanding Native Status and Recovery
- Supervisor Change](/en/native/supervisor-change) - Multi-session Collaboration and sub-change
